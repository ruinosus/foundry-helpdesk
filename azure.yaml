# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
#
# azd configuration — Foundry Helpdesk.
#
# Provision-only for Phase 0: `azd up` runs infra/ (Foundry account + project +
# gpt-4.1-mini + data-plane role). Backend/frontend run locally (see README), so
# no services are declared yet — container hosting arrives in Phase 6.
name: foundry-helpdesk
metadata:
  template: foundry-helpdesk@0.1.0
