{
  "name": "foundry-assured-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end tests for Foundry Assured (deployed cloud app). Drives the real UI through Entra sign-in + each domain, capturing screenshots/trace/video to ./artifacts.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report artifacts/report",
    "install:browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  },
  "dependencies": {
    "otplib": "^12.0.1"
  }
}
