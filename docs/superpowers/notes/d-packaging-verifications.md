# Sub-project D-packaging — runtime / SDK verifications (plan Task 0)

Verified on `feature/saas-d-packaging` against the installed backend venv (`apps/backend/.venv`, Python 3.12): `agent-framework` **1.9.0**, `azure-ai-projects` **2.2.0**, `agent_framework_foundry_hosting` **NOT installed in the backend venv**. This is the gate for D-packaging Phase 1: it pins what Tasks 1–3 may implement now against verified surface vs what must be fenced `TODO(infra-gated)`.

Supersedes the relevant rows of `d-verifications.md` where the installed surface has changed: notably, the `invocations` protocol literal **is now present** in `azure-ai-projects` 2.2.0 enums (it was absent at D-runtime time), but there is still **no first-party Invocations client** and **no SSE envelope schema** offline.

| Unknown | Finding | Impact (Tasks 1–3: implement now vs fence) |
|---|---|---|
| **Does `InvocationsHostServer` exist + its constructor signature? Does it wrap an agent like `ResponsesHostServer(workflow_agent)`?** | **Not determinable in the backend venv — the package is not installed there.** `import agent_framework_foundry_hosting` → `ModuleNotFoundError`; `importlib.metadata.version("agent-framework-foundry-hosting")` → `PackageNotFoundError`. It is a **hosted-image-only dep**: declared in `apps/hosted-agent/requirements.txt` and `apps/hosted-cockpit/requirements.txt` (`agent-framework-foundry-hosting`), NOT in `apps/backend/pyproject.toml`. The only host-server use in the repo is `ResponsesHostServer(workflow_agent)` (`apps/hosted-agent/main.py:107`, `apps/hosted-cockpit/main.py:86`), where the arg is a `WorkflowBuilder(...).build().as_agent()` — i.e. a `SupportsAgentRun` agent. Whether an `InvocationsHostServer` class exists and whether its `__init__` mirrors `ResponsesHostServer(agent)` could **not** be confirmed offline (the dist is resolved only inside the container build). Per project rule #1, the constructor signature is NOT fabricated here. | **INFRA-GATED.** The container's host-server line for the platform agent is a hosted-image concern, configured at deploy/build (the container installs `agent-framework-foundry-hosting`). Task 1 may scaffold a new `apps/hosted-platform/main.py` mirroring `apps/hosted-agent/main.py`, but the `from agent_framework_foundry_hosting import InvocationsHostServer` + `InvocationsHostServer(agent)` line must be fenced `TODO(infra-gated): verify InvocationsHostServer signature against the installed hosted image` — do NOT pin a signature in backend code or assert it equals `ResponsesHostServer(agent)`. The agent it wraps (`build_platform_agent()`) IS verified (next row). |
| **Does `build_platform_agent()` return something a host server can wrap?** | **YES — verified.** `build_platform_agent()` (`app/agents/platform.py:30`) returns `client.as_agent(name=..., description=..., instructions=..., tools=...)` where `client` is `FoundryChatClient` — i.e. an `Agent` (the same `as_agent()` shape `apps/hosted-agent/main.py` feeds to `ResponsesHostServer`). It satisfies `SupportsAgentRun` (id/name/description + run/create_session/get_session; see the `PerRequestPlatformAgent` docstring at `platform.py:46`). So a host server can wrap it. The catch: the hosted variant is **single-identity** (platform agent identity via `DefaultAzureCredential`), so it must NOT call `build_platform_agent()` as-is (that reads per-request OBO/roles from a request contextvar that won't exist in the hosted request/response model) — the hosted build must construct its own agent with hosted-path tools. | **IMPLEMENT NOW (shape) + fence the tool wiring.** Task 1 can build a self-contained hosted platform agent the way `apps/hosted-agent/main.py` does for helpdesk: `FoundryChatClient(...).as_agent(name="PlatformConcierge", instructions=PLATFORM_INSTRUCTIONS, tools=<hosted tools>)`. The `as_agent(...)` API is verified (1.9.0). The TOOLS list is the next row. |
| **Toolbox / MCP-connection + OAuth-passthrough config surface in `azure-ai-projects` 2.2.0** | **Largely present + verified offline.** (1) **Toolbox CRUD:** `BetaToolboxesOperations` exists (`create_version`, `delete`, `delete_version`, `get`, `get_version`, `list`, `list_versions`, `update`), reached via `project.beta.toolboxes` (`AIProjectClient.beta` is a `BetaOperations`, `_client.py:102`; `toolboxes` is set in its `__init__`). Models: `ToolboxObject(id, name, default_version)`, `ToolboxVersionObject`, `ToolboxSkill`, `ToolboxSkillReference(type, name, version)`, `ToolboxPolicies`, `UpdateToolboxRequest`. (2) **MCP tool / connection surface:** `MCPTool` fields are `server_label, server_url, connector_id (Literal['connector_dropbox'|'connector_gmail'|'connector_googlecalendar'|'connector_googledrive'|'connector_microsoftteams'|'connector_outlookcalendar'|'connector_outlookemail'|'connector_sharepoint']), authorization (Optional[str]), headers, allowed_tools (list[str] | MCPToolFilter), require_approval (MCPToolRequireApproval | 'always' | 'never'), project_connection_id (Optional[str]), tool_configs`. So **OAuth identity passthrough per connection is declared as DATA** — via `project_connection_id` (a named Foundry connection) and/or `connector_id` + `authorization`, NOT hand-rolled credential code. (3) **Identity-passthrough credential type:** `AgenticIdentityPreviewCredentials` (`CredentialType.AGENTIC_IDENTITY_PREVIEW = 'AgenticIdentityToken_Preview'`) and `EntraAuthorizationScheme` exist among `azure.ai.projects.models`. **(4) agent-framework client helper:** `FoundryChatClient.get_mcp_tool(*, name, url=None, description=None, approval_mode=Literal['always_require','never_require']|dict[str,list[str]]|None, allowed_tools=None, headers=None, project_connection_id=None, **kwargs) -> FoundryMCPTool` — verified signature; this is exactly what `build_hosted_from_connections(conns, roles, get_tool)` (`app/agents/mcp/tools.py:184`) is already written to call (`get_tool(name=..., url=..., allowed_tools=..., approval_mode=..., project_connection_id=...)`). What is **NOT** determinable offline: the exact wire-up that binds a Toolbox to a hosted agent at deploy, and which named connection / `connector_id` each platform MCP server maps to in a given Foundry project (those are deployment-time facts). | **IMPLEMENT NOW (the passthrough is DATA, per ADR-011 / rule #6):** Task 2 wires hosted tools via the **already-written** `build_hosted_from_connections(...)` passing `FoundryChatClient.get_mcp_tool` as `get_tool` (signature verified) — OAuth identity flows through `project_connection_id`, NO header_provider, NO hand-rolled credential code. The role filter / URL resolution in `_connection_build_params` (`tools.py:124`) is reused unchanged. **FENCE as infra-gated:** the **Toolbox-by-name reference** and the per-connection mapping (`project_connection_id` values, `connector_id` choices, `AgenticIdentityPreviewCredentials`/`EntraAuthorizationScheme` selection) are **configured at deploy and documented in the runbook**, referenced by name/env — NOT created in backend code. Any code that *creates/updates* a Toolbox via `project.beta.toolboxes.*` must carry `TODO(infra-gated): confirm Toolbox<->hosted-agent binding against deployed project` (the CRUD API is verified; the binding semantics are not). |
| **Bridge (client) side: first-party Invocations client OR raw `httpx`/`aiohttp` POST? Auth scope? Is `httpx` a dep?** | **No first-party Invocations client offline — must be a raw authenticated POST.** `AIProjectClient` public surface is only `close`, `get_openai_client`, `send_request` — there is **no** `invoke`/`agents`/`run` method. `get_openai_client(agent_name=..., allow_preview=True)` builds the **OpenAI/Responses** shape only: the endpoint template hard-codes `protocols/openai` (`_patch.py:51`: `…/agents/{agent_name}/endpoint/protocols/openai`; `aio/_patch.py:150`). The `invocations` protocol literal **now exists** as an enum value (`AgentEndpointProtocol.INVOCATIONS = "invocations"` and `AgentProtocol.INVOCATIONS`, `models/_enums.py:85,128`; plus `INVOKE_AGENT_INVOCATIONS_API` routine-dispatch enums at `:868`), and there are payload models `InvokeAgentInvocationsApiDispatchPayload` / `InvokeAgentInvocationsApiRoutineAction` (opaque `*args/**kwargs` constructors). But there is **no client method that builds a `protocols/invocations` URL** (`grep "protocols/invocations"` → 0 hits across the installed SDK) and **no SSE event/envelope schema** for it. So the bridge must POST to `{endpoint}/agents/{name}/endpoint/protocols/invocations` itself, by analogy to the verified `/openai` template. **Auth scope:** the existing Responses bridge (`app/services/hosted.py:33`) lets `get_openai_client` handle auth via `DefaultAzureCredential`/`allow_preview=True`; for a raw POST the bearer is the Foundry data-plane scope — **`https://ai.azure.com/.default`** (the Foundry/AI Projects audience; cross-checked against the Responses client which targets the same project endpoint) — note this is **NOT pinned by an SDK constant offline**, so treat the literal scope as `TODO(infra-gated): confirm against deployed agent`. **`httpx` is already available** — it is NOT a direct dep in `apps/backend/pyproject.toml`, but it is a transitive dep present in `uv.lock` (12 references; it's pulled in by the OpenAI/azure stack), so `import httpx` works in the venv. The relay is a **passthrough**: the response bytes are AG-UI already (the hosted platform agent serves the AG-UI/Invocations protocol), so the bridge re-emits bytes untouched — no envelope parsing, no re-encoding (unlike `stream_agui`, which re-encodes Responses→AG-UI). | **IMPLEMENT NOW (URL + passthrough skeleton already in place):** `_platform_invocations_url()` (`hosted.py:102`) already builds the correct template, and `stream_platform_agui` (`hosted.py:116`) is the clean-error skeleton. Task 3 may implement the raw `httpx` streaming POST (`httpx.AsyncClient().stream("POST", url, headers={Authorization: Bearer <token>}, content=<AG-UI run body>)`) relaying SSE bytes 1:1, BUT must fence two literals as `TODO(infra-gated)`: (a) the **auth scope** string (`https://ai.azure.com/.default` is the best-evidence value, not SDK-confirmed) and (b) the **exact request body / SSE framing** — the AG-UI run-input envelope the deployed agent expects is not verifiable offline. Do NOT fabricate the request envelope; the passthrough relay (re-emit response bytes) needs no envelope knowledge, but the *request* POST body does. Real end-to-end streaming lands against the deployed agent (infra-gated E2E). |

**Net for Phase 1.**

- **`InvocationsHostServer`:** INFRA-GATED. Not in the backend venv (hosted-image-only). Scaffold the hosted-platform container mirroring `apps/hosted-agent/main.py`; fence the `InvocationsHostServer(agent)` import/construction line — do NOT assert its signature equals `ResponsesHostServer(agent)`. The wrapped agent (`as_agent(...)`) IS verified.
- **Toolbox / OAuth passthrough:** the SDK surface is VERIFIED (`project.beta.toolboxes` CRUD, `MCPTool.project_connection_id`/`connector_id`/`authorization`, `AgenticIdentityPreviewCredentials`, and `FoundryChatClient.get_mcp_tool(... project_connection_id=...)` matching the existing `build_hosted_from_connections`). The passthrough is **DATA** (per rule #6 / ADR-011): wire tools through `get_mcp_tool` + `project_connection_id`, no hand-rolled credentials. The **Toolbox-by-name binding to the hosted agent** and the per-connection mapping are INFRA-GATED → runbook/deploy, referenced by name/env, fenced in any create/update code.
- **Bridge client:** NO first-party Invocations client offline → **raw `httpx` POST** (httpx is present transitively, import works). URL template verified by analogy to `/openai`. Passthrough relay of AG-UI bytes needs no envelope parsing. Two literals stay INFRA-GATED: the **auth scope** (`https://ai.azure.com/.default`, best-evidence) and the **request body / SSE framing** (not verifiable offline; do not fabricate per rule #1).
